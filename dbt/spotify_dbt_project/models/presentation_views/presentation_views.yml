version: 2

models:
  - name: track_genre_summary_v1
    description: Summary of genre parent categories, including total number per category per playlist
      and breakdown of which genres each category represents. 